spring:
  datasource:
    url: jdbc:mysql://localhost:3306/muldata?useUnicode=true&characterEncoding=utf-8&useSSL=true&verifyServerCertificate=false
    driver-class-name: com.mysql.jdbc.Driver
    username: root
    password: root
mybatis:
  mapper-locations: classpath:mybatis/mapper/*.xml
  type-aliases-package: com.vueapiserver.shop.entity
server:
  port: 8082
